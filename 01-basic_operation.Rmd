# 기초 셈법

---

## 사칙연산

```{r}
tibble(
  기호 = c("[+]", "[-]", "[*]", "[%]"),
  설명 = c("덧셈", "뺄셈", "곱셈", "나눗셈")
) %>% kable()
```

## 기초 기술통계

```{r}
tibble(
  명령어 = c("sum", "mean", "max", "min"),
  설명 = c("총합", "평균", "최대값", "최소값")
) %>% kable()
```

